var mostWordsFound = function(sentences) {
    let c=0
    for(let i=0;i<sentences.length;i++){
        let b=sentences[i].split(' ')
        if(b.length>c){
            c=b.length
        }
    }
        return c
};
